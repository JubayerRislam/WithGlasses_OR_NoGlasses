# WithGlasses_OR_NoGlasses
A Streamlit app that classifies images of people as wearing glasses or not using a pre-trained Keras model(Which done by me). Upload an image to get instant predictions. Includes `app.py`, `glasses_classifier.keras`, and `requirements.txt` for easy local setup and deployment.
